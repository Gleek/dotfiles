# Updated
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

##For mpc:
set $term urxvt
set $music "urxvt -title floating_term -e ncmpcpp"
set $notes emacsclient -c ~/Dropbox/org-files/Notes.org -F '((name . "Notes"))'
set $perf "terminator -b -e gotop --title=floating_term"
# set $ranger "termite -e ranger"
# set $ranger "terminator -b -e ranger --title=floating_term"

set $ranger "urxvt -title floating_term -e ranger"
# set $pause --no-startup-id mpc toggle # playerctl pause
set $pause --no-startup-id pplayerctl play-pause
# set $play --no-startup-id mpc toggle  # playerctl play
set $play --no-startup-id pplayerctl play-pause # playerctl play
# set $truepause --no-startup-id mpc pause
set $truepause --no-startup-id pplayerctl pause
# set $next --no-startup-id mpc next # playerctl next
set $next --no-startup-id pplayerctl next
# set $prev --no-startup-id mpc prev # playerctl previous
set $prev --no-startup-id pplayerctl previous
# set $lilfor --no-startup-id mpc seek +10
set $lilfor --no-startup-id pplayerctl position 10+
# set $bigfor --no-startup-id mpc seek +120
set $bigfor --no-startup-id pplayerctl position 120+
# set $lilbak --no-startup-id mpc seek -10
set $lilbak --no-startup-id pplayerctl position 10-
# set $bigbak --no-startup-id mpc seek -120
set $bigbak --no-startup-id pplayerctl position 120-
# set $beg --no-startup-id mpc seek 0%
set $beg --no-startup-id pplayerctl position 0
set $truemute pactl set-sink-mute 0 on
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Hack 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
# font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
focus_follows_mouse no
# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal
# bindsym $mod+Return exec terminator -b -e "screen -dR session" --title "floating_term" # Start terminal in a screen
bindsym $mod+Return exec urxvt -title floating_term -e screen -dR session  # Start terminal in a screen
bindsym $mod+Shift+Return exec urxvt -title small_floating_term -e qalc
# bindsym $mod+Shift+Return exec rofi -show calc -modi calc:~/.config/rofi/scripts/calc.sh -font 'Hack 13'
# bindsym $mod+Shift+Return exec terminator -b -e "scim .temp.csv" --title "small_floating_term" # Start a node session
bindsym $mod+m exec $music
bindsym $mod+Shift+m [instance="deadbeef"] scratchpad show
bindsym $mod+Shift+f exec $ranger

bindsym $mod+n exec $notes
# kill focused window
bindsym $mod+q kill
# start dmenu (a program launcher)
bindsym $mod+d exec "rofi -modi combi -show combi -combi-modi window,drun,run"
bindsym $mod+Shift+d exec "~/.config/rofi/scripts/rofi.sh"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
# bindsym $mod+j focus left
# bindsym $mod+k focus down
# bindsym $mod+l focus up
# bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
# bindsym $mod+Shift+j move left
# bindsym $mod+Shift+k move down
# bindsym $mod+Shift+l move up
# bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child
# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show


set $workspace1 "1"
set $workspace2 "2"
set $workspace3 "3"

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10


# move focused workspace to screen
bindsym $mod+Ctrl+Left move workspace to output left
bindsym $mod+Ctrl+Right move workspace to output right
bindsym $mod+Ctrl+Up move workspace to output up
bindsym $mod+Ctrl+Down move workspace to output down

assign [class="Firefox"] $workspace1
assign [instance="emacs"] $workspace2
# assign [class="Rhythmbox"] $workspace3

# Authy
for_window [instance="crx_gaedmjdfmmahhbjefcbgaolhhanlaolb"] floating enable

# Emacs Notes
for_window [instance="Notes"] floating enable

# Pritunl
for_window [instance="pritunl"] floating enable

# Dragger
for_window [instance="dragger"] floating enable

# MPV
for_window [class="mpv"] floating enable
for_window [class="mpv"] resize set 800 600

# for_window [class="keepassx"] floating enable
for_window [title="floating_term"] floating enable

for_window [title="floating_term"] resize set 800 600
for_window [title="floating_term"] move position center

for_window [title="small_floating_term"] floating enable
for_window [title="small_floating_term"] resize set 600 300
for_window [title="small_floating_term"] move position center

for_window [title="tiny_floating_term"] floating enable
for_window [title="tiny_floating_term"] resize set 300 200
for_window [title="tiny_floating_term"] move position center

for_window [instance="copyq"] floating enable
for_window [instance="copyq"] move position center

for_window[title="*SmartJump Peek*"] floating enable

for_window[instance="deadbeef"] move scratchpad

# for_window [instance="dropdown"] resize set 625 400
# for_window [instance="dropdown"] move position center
# reload the configuration file
bindsym $mod+c exec copyq show
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+Shift+k exec "keepassxc"



# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Multi screen mode
set $screen-mode "Dual Monitor (D), Dual Vertical (V), Single Monitor (S)"
mode $screen-mode {
     bindsym $mod+d exec ./.config/i3/screen-dual.sh;  mode "default"
     bindsym $mod+s exec ./.config/i3/screen-single.sh;  mode "default"
     bindsym $mod+v exec ./.config/i3/screen-vertical.sh;  mode "default"
     bindsym Return mode "default"
     bindsym Escape mode "default"
}
bindsym $mod+XF86LaunchB mode $screen-mode

 #Log out Reboot and PowerOFF
mode "Exit (L)ogout, (R)eboot, (P)oweroff, (S)Suspend" {
    bindsym $mod+r exec systemctl reboot
    bindsym $mod+l exit
    bindsym $mod+p exec systemctl poweroff
    bindsym $mod+s exec ./.config/i3/lock.sh & systemctl suspend -i
    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+p mode "Exit (L)ogout, (R)eboot, (P)oweroff, (S)Suspend"

set $bg-color            #2f343f
set $inactive-bg-color   #2f343f
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935
set $greenindic          #00ff00
set $light-grey          #808080
# window colors
#                       border              background         text                 indicator
# client.focused          $bg-color           $bg-color          $text-color          #00ff00
client.focused          $light-grey           $light-grey          $light-grey        $bg-color
client.unfocused        $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
client.focused_inactive $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          #00ff00

hide_edge_borders smart
for_window [class="^.*"] border pixel 1
new_window 1pixel
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3blocks -c ~/.config/i3/i3blocks.conf
        # status_command polybar example
        # tray_output primary
}

bindsym $mod+x exec ~/.config/i3/lock.sh

bindsym $mod+grave exec ~/.config/i3/flash.sh
bindsym $mod+Shift+grave exec transset-df -a -m 0 -x 0.5


# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.config/i3/volume.sh inc #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id ~/.config/i3/volume.sh dec #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id ~/.config/i3/volume.sh mute # mute sound

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec light -A 2 # increase screen brightness
bindsym XF86MonBrightnessDown exec light -U 2 # decrease screen brightness
bindsym $mod+XF86MonBrightnessUp exec light -A 0.3 # increase screen brightness slowly
bindsym $mod+XF86MonBrightnessDown exec light -U 0.3 # decrease screen brightness slowly

# Media player controls
bindsym XF86AudioPlay exec $play
bindsym XF86AudioPause exec $pause
bindsym XF86AudioNext exec $next
bindsym XF86AudioPrev exec $prev
bindsym $mod+XF86AudioNext exec $lilfor
bindsym $mod+XF86AudioPrev exec $lilbak
bindsym $mod+Shift+XF86AudioNext exec $bigfor
bindsym $mod+Shift+XF86AudioPrev exec $bigbak

#screenshot
# bindsym $mod+XF86LaunchA --release exec scrot -s "/home/umar/Pictures/Screenshots/Screenshot from `date '+%F %X'`.png"
bindsym $mod+XF86LaunchA --release exec flameshot gui --path "/home/umar/Pictures/Screenshots/"

bindsym $mod+Shift+x --release exec xkill
# bindsym $mod+backslash exec $term -e htop
bindsym $mod+backslash exec $perf
bindsym KP_Enter exec $perf
bindsym $mod+Shift+backslash exec $term -title floating_term -e nethogs wlp3s0
#STOP/HIDE EVERYTHING
bindsym $mod+Shift+BackSpace exec $truemute ; exec $truepause ; workspace lmao ; exec $term -e htop ; exec $term -e ranger
exec_always feh --bg-scale /home/umar/.wallpaper.jpg
exec /home/umar/.keyboard.sh
exec xinput --set-prop bcm5974 "libinput Natural Scrolling Enabled" 1
exec emacs
exec --no-startup-id compton -bcCGf -i 0.8 --no-fading-openclose --mark-ovredir-focused --sw-opti
exec --no-startup-id nm-applet
# Auto lock
# exec --no-startup-id xautolock -time 3 -locker ~/.config/i3/lock.sh -corners -000
exec --no-startup-id dropbox start
# exec --no-startup-id megasync
exec --no-startup-id mpd
exec --no-startup-id mpDris2
exec --no-startup-id deadbeef
exec --no-startup-id copyq
exec --no-startup-id redshift
exec --no-startup-id ~/.config/i3/battery_alert.sh -n # Notification on low battery
# exec_always /home/umar/.config/polybar/launch.sh
